pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                // Simula un build con un archivo
                sh 'cat prueba.txt'
            }
        }
    }
}
