pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                // Simula un build con un archivo
                s 'cat prueba.txt'
            }
        }
    }
}
